services:

  db_processing:
    image: mysql:8
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: ProcessingDB
      MYSQL_PASSWORD: root
    ports:
      - "3307:3306"
    volumes:
      - mysql_bkp_processing:/var/lib/mysql
    networks:
      - kafka_default

#  kafka_processing:
#    image: apache/kafka:latest
#    container_name: kafka_processing
#    hostname: kafka_processing
#    ports:
#      - "9093:9092"
#      - "39092:9092"
#    environment:
#      KAFKA_ENABLE_KRAFT: yes
#      KAFKA_NODE_ID: 1
#      KAFKA_BROKER_ID: 1
#      KAFKA_PROCESS_ROLES: 'broker,controller'
#      KAFKA_LISTENERS: 'PLAINTEXT://:9092, CONTROLLER://:9093, PLAINTEXT_DOCKER://:39092'
#      KAFKA_ADVERTISED_LISTENERS: 'PLAINTEXT://localhost:9092,PLAINTEXT_DOCKER://kafka_processing:39092'
#      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: CONTROLLER:PLAINTEXT,PLAINTEXT:PLAINTEXT,SSL:SSL,SASL_PLAINTEXT:SASL_PLAINTEXT,SASL_SSL:SASL_SSL,PLAINTEXT_DOCKER:PLAINTEXT  # Security protocol for listeners
#      KAFKA_CONTROLLER_QUORUM_VOTERS: '1@localhost:9093'
#      KAFKA_INTER_BROKER_LISTENER_NAME: 'PLAINTEXT'
#      KAFKA_CONTROLLER_LISTENER_NAMES: 'CONTROLLER'
#      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
#
#    volumes:
#      - kafka_data:/nitnami/kafka
#    networks:
#      - kafka_default
#
#  kafka-init:
#    container_name: init-processing-kafka
#    image: apache/kafka:latest
#    depends_on:
#      - kafka_processing
#    command:
#      - /bin/sh
#      - -c
#      - |
#        sh /opt/kafka/bin/kafka-topics.sh --bootstrap-server kafka_processing:39092 --list
#        echo -e 'Creating kafka topics'
#        sh /opt/kafka/bin/kafka-topics.sh --bootstrap-server kafka_processing:39092 --create --if-not-exists --topic processing_topic --replication-factor 1 --partitions 1
#        echo -e 'Successfully created the following topics:'
#        sh /opt/kafka/bin/kafka-topics.sh --bootstrap-server kafka_processing:39092 --list
#    networks:
#      - kafka_default

  processing-service:
    build: .
    ports:
      - "8082:8082"
#    environment:
#      SPRING_DATASOURCE_URL: jdbc:mysql://db_processing:3306/ProcessingDB?allowPublicKeyRetrieval=true&useSSL=false
#      SPRING_DATASOURCE_USERNAME: root
#      SPRING_DATASOURCE_PASSWORD: root
    depends_on:
      - db_processing
#      - kafka_processing
    networks:
      - kafka_default

volumes:
  mysql_bkp_processing:
  kafka_data:

networks:
  kafka_default:
    driver:
      bridge